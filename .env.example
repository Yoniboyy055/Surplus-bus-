# Supabase Configuration
# Get these values from your Supabase project dashboard: Settings > API

# Your Supabase project URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anon/public key (safe to use in client-side code)
# This key respects Row Level Security (RLS) policies
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role key (NEVER expose this in client-side code!)
# This key bypasses RLS - use only in server-side code
# Keep this secret and never commit it to version control
SUPABASE_SERVICE_ROLE_KEY=
