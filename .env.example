# =============================================================================
# Surplus Referral Platform - Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# Get these from your Supabase project settings
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key - ONLY used server-side for admin operations
# Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# Owner Configuration
# The owner email gets automatic operator access
# -----------------------------------------------------------------------------
OWNER_EMAIL=nohabe056@gmail.com

# -----------------------------------------------------------------------------
# Application URL (optional)
# Used for generating referral links. Defaults to localhost in dev.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Agent/Cron Configuration
# Secret for authenticating cron job requests to agent endpoints
# Generate with: openssl rand -hex 32
# -----------------------------------------------------------------------------
CRON_SECRET=your-cron-secret-here

# In Vercel:
# 1. Add CRON_SECRET to your Environment Variables
# 2. Vercel automatically validates cron requests when this is set
# 3. The x-vercel-cron header is added automatically to cron requests
